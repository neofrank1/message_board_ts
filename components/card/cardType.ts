type CardHeaderType = {
    className: string;
    children: React.ReactNode;
};

type CardBodyType = {
    className: string;
    children: React.ReactNode;
};

type CardFooterType = {
    className: string;
    children: React.ReactNode;
};

export type { CardFooterType, CardHeaderType, CardBodyType };